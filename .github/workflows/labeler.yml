name: PR Branch Labeler
on:
- pull_request_target

jobs:
  label_prs:
    permissions:
      contents: read
      pull-requests: write
    runs-on:  ubuntu-latest
    steps:
    - uses: actions/labeler@v4
